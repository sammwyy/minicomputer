[package]
name = "minicomputer-worker"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
rmp-serde = "1"
tokio = { version = "1", features = ["macros", "net", "io-util", "signal", "process", "rt-multi-thread"] }
